{
    // 使用 IntelliSense 了解相关属性。 
    // 悬停以查看现有属性的描述。
    // 欲了解更多信息，请访问: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Python: 当前文件",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "justMyCode": true,
            // "cwd": "${workspaceFolder}", // 
            // "cwd": "${fileDirname}", //当前路径设置为运行的代码所在的文件夹
            "env": {"PYTHONPATH":"${workspaceFolder};${env:PYTHONPATH};"} // 将项目根目录加入搜索路径中，便于引用模块
        }
    ]
}